import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    tituloImage: {
        paddingTop: 8,
        paddingBottom: 8,
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center'
    },
    svgImage: {
        alignItems: 'center'
    }
})

export default styles;

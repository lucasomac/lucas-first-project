import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    tituloImage: {
        paddingTop: 10,
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center'
    }
})

export default styles;
